// Firebase configuration file - No longer used
// The application now uses localStorage for data persistence to simplify hosting
// This file is kept for reference only and is not loaded by the application

/*
const firebaseConfig = {
    // Add your Firebase config here if you decide to use Firebase in the future
};

firebase.initializeApp(firebaseConfig);
*/
